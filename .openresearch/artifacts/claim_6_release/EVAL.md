# Evaluation

The release audit exits zero only when it correctly diagnoses the missing
capabilities. The separate claim verifier exits nonzero with status BLOCKED.
A false-completeness negative inventory must also exit nonzero.
