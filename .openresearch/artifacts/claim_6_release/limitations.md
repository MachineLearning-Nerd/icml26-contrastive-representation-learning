# Limitations

Missing artifacts establish BLOCKED, not FALSIFIED. A failed attempt with a
guessed configuration would violate the exact-claim and non-circularity gates.
The generic FastCLIP repository is useful infrastructure but cannot identify
the paper's data subsets or reproduce its raw measurements.
