# Method

The checker verifies the triangle inequality directly, supplies a counterexample to equality, differentiates the SCRL and SSCRL bound terms, and checks the leading balance points. This tests the paper's bound-level explanation, not a nearby empirical additivity proxy.

The negative control asserts equality and must exit nonzero.
