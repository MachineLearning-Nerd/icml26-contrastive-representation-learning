# Exact command

`uv sync --frozen && uv run --frozen python run_reproduction.py`

Formal run: Hugging Face `cpu-upgrade`; estimated scientific need one core. Actual allocation and runtime are emitted in raw output.
