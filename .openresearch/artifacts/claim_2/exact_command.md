# Exact command

`uv sync --frozen && uv run --frozen python run_reproduction.py`

Formal run: Hugging Face `cpu-upgrade`, immutable uv CPU image. Estimated scientific allocation: one core; platform allocation and measured runtime are printed by the runner.
