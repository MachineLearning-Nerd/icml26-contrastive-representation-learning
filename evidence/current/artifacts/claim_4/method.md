# Method

The symbolic checker verifies both polynomial exponents, preserves the shared-negative assumption and uniform high-probability quantifiers, and requires explicit disclosure of logarithmic factors. The negative control substitutes SCRL-style independent sampling and must fail.
